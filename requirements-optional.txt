# Optional heavy dependencies for the semantic-text (embeddings) and image (CLIP)
# phases. These pull in torch (~2 GB) and are NOT installed into the Docker image.
# Install locally only when using PSS_TEXT_BACKEND=embeddings or the image phase:
#     .venv/bin/pip install -r requirements-optional.txt

sentence-transformers==3.3.1
pillow==11.0.0
