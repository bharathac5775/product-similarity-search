# Core runtime dependencies for Part 1, Part 2, and Part 3 (FAISS).
# This is the ONLY file installed into the Docker image, keeping it lean.

# Core data + ML
pandas==2.2.3
numpy==2.1.3
scikit-learn==1.5.2

# API + storefront templates
fastapi==0.115.6
uvicorn[standard]==0.34.0
jinja2==3.1.6
python-multipart==0.0.20

# Part 3 (vector search)
faiss-cpu==1.9.0

# Dev / test
pytest==8.3.4
httpx==0.28.1
requests==2.32.3
